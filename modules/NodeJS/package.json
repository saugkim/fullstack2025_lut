{
  "name": "memo",
  "version": "1.0.0",
  "description": "nodejs crash course",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon --env-file=.env server.js",
    "test2": "node --env-file=.env server22.js"
  },
  "author": "kim",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.1.10"
  },
  "dependencies": {
    "env": "^0.0.2"
  }
}
